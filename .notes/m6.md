# M6 — Paging (identity-mapped virtual memory + page-fault handler)

## Result: PASS

`zig build` clean. Boot test (`bash tools/qrun.sh zig-out/bin/kernel.elf "go\n" 10`):

```
[M6] paging enabled (identity map, CR0.PG on)
[M6] sentinel @8MiB readback = 0xCAFEBABE
[M6] mapped 0xE0000000->0x1AD000, readback = 0x1234ABCD
nyx: M6 OK
```

All earlier markers (M0–M5) present, no reset/loop. Markers print AFTER CR0.PG is
on, proving the kernel executes mapped code.

